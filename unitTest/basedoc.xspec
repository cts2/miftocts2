<?xml version="1.0" encoding="UTF-8"?>
<x:description xmlns:x="http://www.jenitennison.com/xslt/xspec" stylesheet="none">
    <!-- Identify the MIF baseline file we are running against -->
    <x:param name="vocabModelName" select="resolve-uri('../../source/DEFN=UV=VO=1189-20121121.coremif')"/>

    <!-- Comments have to be switched off for unit tests -->
    <x:param name="useComments" select="false()"/>
</x:description>